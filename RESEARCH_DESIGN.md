# RESEARCH_DESIGN — 지도학습 ECG 디노이징 분석 설계

`README.md` 가 "무엇을 어떻게 구현했고 결과가 무엇인가"라면, 이 문서는 **"확정 모델
하나로 무엇을 주장하고 그 근거를 어느 산출물이 대는가"** 다. 원고의 Table·Figure 배치와
사전 등록 사항을 담는다.

산출물만 제시한다. 해석·판독·원고 문구는 붙이지 않는다 — 수치가 무엇을 뜻하는지는
결과를 보고 정한다.

---

## 0. 전제 — 확정 모델 하나

탐색(`experiments/` — 손실 16런 · 해상도 4런 · 잔차 4런 · hidden/정규화)을 마치고
**`C16_seed42`** 로 고정했다. `03_bss.py --final` 이 `results/03_bss/selected.json` 에
기록한다.

| 항목 | 값 |
|---|---|
| 구조 | 깊이 4 · 압축률 16 · dilations [1,1,4,32] · 수용영역 9.21초 · 패딩 0 |
| K · 시드 · hidden | 4 · 42 · 64 |
| 손실 | λ_sup 1.0 · γ₁ 50 · γ₂ 0 · β 0 · σ 정규화 없음 |
| 잔차 연결 | 없음 |
| 선정 에폭 | 236 (argmin L_sup^val, ratio 1.5 후보) |

**04 이후는 이 모델만 쓴다.** 단계마다 다른 모델을 쓰면 결과가 연결되지 않는다.
재학습은 어디에도 없다 — 전부 추론 시점 조작이다.

**split** — 탐색은 val(5기록 900분절), 확정 평가는 **test**(9기록 1,620분절)다.
test 는 선정에 쓰이지 않았고 잡음 풀도 train/val 과 다른 구간이다.

**복원의 주 노선은 B** = D(z_clean, 0, 0, 0). A(성분차감)는 원본 x_noisy 를 유지한 채
모델 추정치만 빼므로 처리 전과 출발선이 같지 않다 — 비교선으로만 싣는다.

---

## 1. 주장의 사슬

| # | 주장 | 근거 산출물 | 원고 자리 |
|---|---|---|---|
| ① | 인코더가 배정된 소스를 실제로 담는다 | `03_bss/` 배정 대각 + 대응표 | Table 1 · Figure 2 |
| ② | 잡음 인코딩을 끄면 신호가 복원된다 — 고전 방법보다 낫다 | `04_.../three_ways` + `breakdown` | Table 2 · Figure 3a |
| ③ | 복원이 파형으로도 확인된다 | `05_figure/` 겹침 12장 | Figure 3b |
| ④-1 | **참값 없이 봐도** 품질이 올랐다 | `06_.../sqi_summary` + `sqi_compare` | Table 3 · Figure 4a |
| ④-2 | 임상 측정값이 참값에 가까워졌다 (ST) | `06_.../metric_summary` | Table 4 · Figure 4b |
| ⑤ | 같은 파이프라인이 외부에서도 돈다 | `07_.../sqi_summary`·`morphology` | Table 5 · Figure 5 |

①이 성립해야 ②의 "끄기"가 정당하고, ②가 되어야 ③④가 볼 것이 있고, ④-1 이
④-2 와 ⑤를 잇는다 — SQI 는 test 에서 참값과 대조해 검증했으므로 참값 없는 외부에서도
같은 뜻으로 읽을 수 있다.

---

## 2. 단계별 설계 요약

지표의 수식·정의는 README §5 에 있다. 여기는 각 단계가 무엇을 주장에 보태는지만 적는다.

### 03 — 성분 정렬 (Table 1)

배정 대각의 주 지표 4종(|r|·RMSE_norm·MAD·F1)과 누출비. 배정이 사전에 정해져 있으므로
대각이 곧 주장 ①의 근거다. r_QRS 는 clean 행에만 싣는다.

**하지 않는 것** — 표본 수 기반 p값 · 순열 검정 · 코히런스 · 인코더에 참조 이름 붙이기.

### 04 — 복원 (Table 2)

주 지표는 SNR/ΔSNR·PRD. 비교 대상 7상태 — B(주) · x_noisy(기준선) · 고전 3종(대역통과 ·
웨이블릿 임계값 · 웨이블릿+기저선제거) · A(보조) · M0(참고). **breakdown** 이 입력 SNR
구간별·기록별 재집계로 "언제 유효한가"를 댄다.

### 05 — 파형 (Figure 3b)

B 의 corr 순위로 상위 4 · 중간 4 · 하위 4. **결과를 보고 고른 사례**임을 제목에 밝히고,
좋은 사례만 싣지 않으려 하위 4를 함께 낸다.

### 06 — 활용 효과 (Table 3·4, Figure 4)

① SQI 5종(basSQI·pSQI·kSQI·bSQI·ECGMeanCoef) — 참값 불필요. 처리 전/후에서 각각 독립
산출하고 참값은 기준선(점선)으로만 둔다.
② 임상 형태 지표(ST60·ST80·R진폭·QRS면적) — 기준점(R-피크)을 x_clean 에서 한 번 검출해
세 신호에 공유, 박동별 오차를 낸다. **QT·QTc 는 판별력이 없어 제외** (근거: 상용 판독기
간 차이 6–10 ms vs 우리 절대오차 47–64 ms).

### 07 — 외부 (Table 5, Figure 5)

재학습 없이 vitaldb·mimic_iv·galaxyppg 에 적용. **참값이 필요 없는 층만 낸다** —
SQI 5종, 임상 지표의 값(오차가 아니다), 성분 대응, 스펙트럼. 분절 선정은 사전 규칙,
진폭은 학습 입력 SD 중앙값에 정합. 정량 성능은 주장하지 않는다.

---

## 3. 원고 배치 요약

| 요소 | 내용 | 출처 |
|---|---|---|
| Table 1 | 배정 대각 — 주 지표 4종 + 누출비 | `03_bss/.../assignment_diagonal.csv` |
| Table 2 | 복원 7상태 × 주 2 + 보조 5 | `04_.../three_ways.csv` |
| Table 3 | SQI 5종 — 전/후/참값 | `06_.../sqi_summary.csv` |
| Table 4 | 임상 지표 오차 — 전/후, 개선 비율 | `06_.../metric_summary.csv` |
| Table 5 | 외부 3소스 SQI | `07_.../sqi_summary.csv` |
| Figure 1 | 파이프라인 도식 + 배정 | (작성 예정) |
| Figure 2 | 대응 히트맵 · 확장 지표 4패널 | `03_bss/.../figures/` |
| Figure 3 | 복원 겹침 (a 전체 지표, b 대표 분절) | `04_.../three_ways_overlay` · `05_.../seg_*` |
| Figure 4 | a SQI 전/후 분포·이동 · b 임상 오차 | `06_.../figures/` |
| Figure 5 | 외부 겹침 (입력 위에 B) | `07_.../figures/overlay_top*` |
| 부록 | 탐색 전체(손실 16런 · 해상도 4런 · 잔차 4런) · SNR 구간 분해 · 나머지 표 | `experiments/` · `breakdown.csv` |

**부록에 실을 설계 근거** (README §6) — 압축률 16 선택(4런 단조 개선, 파라미터 15배 감소),
γ₁=50(참조 에너지비 측정), 잔차 미채택(성분 경로 분해 — 심장 정보가 인코딩이 아니라
잔차에 실림), R진폭·QRS면적이 개선되지 않는 구조적 이유(차분 측정의 잡음 상쇄).

---

## 4. 사전 등록 사항

결과를 보기 **전에** 확정한 것들이다. 결과를 보고 바꾼 것은 그 사실과 근거를 명시한다.

1. **체크포인트 선택** — argmin L_sup^val, 후보는 ≤1.5×min 인 판정 에폭(2에폭 간격).
   민감도 {1.2, 1.5, 2.0} 병기.
2. **조기 종료** — val_sup 50에폭 정체, 최대 300에폭.
3. **인코더-소스 배정** — enc1 clean · enc2 bw · enc3 ma · enc4 em. 사후에 바꾸지 않는다.
4. **split** — 탐색 val, 확정 test. test 는 선정에 쓰지 않는다.
5. **06 기준점** — x_clean 에서 검출해 세 신호에 공유. 가장자리 400 ms 박동 제외.
6. **06 측정 창** — 기저선 [R−80,−40]ms · J점 R+40ms · ST 60/80ms ·
   QRS [−50,+60]ms · R진폭 ±50ms.
7. **07 분절 선정** — 사전 규칙(기록 앞 10분 · NaN · 평탄비 · 포화비 · 피크 수 · 심박수 ·
   RR 변동계수). 지표를 보지 않는다.
8. **사후 선정임을 밝히는 곳** — 03 상위 3분절, 05 의 12분절. 제목과 note 에 명시.

**결과를 보고 바꾼 것** (근거 포함)
- QT·QTc 제외 — 판별력 부재 (README §5.5).
- 복원 주 노선 A → B — A 는 처리 전과 출발선이 다름 (README §3.3).
- SQI 층 신설 — 참값 의존을 우회하는 독립 근거 필요 (README §5.4).
