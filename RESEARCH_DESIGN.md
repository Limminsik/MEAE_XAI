# RESEARCH_DESIGN — 지도학습 디노이징 프레임워크 분석 설계

`README.md` 가 "무엇을 어떻게 구현했나"라면, 이 문서는 **"확정 모델 하나로 무엇을 주장하고
그 근거를 어느 산출물이 대는가"** 다. 03~07 이 원고의 어느 자리에 놓이는지까지 미리 정한다.

산출물만 제시한다. 해석·판독·원고 문구는 붙이지 않는다 — 수치가 무엇을 뜻하는지는
결과를 보고 정한다.

---

## 0. 전제 — 확정 모델 하나

02 에서 여덟 런을 돌려 설정을 탐색했다(README §2.1.1). 그중 **하나를 고르면**
`03_bss.py --final` 이 `results/03_bss/selected.json` 에 고정한다.

```json
{ "run": "...", "epoch": 0, "K": 4,
  "supervise": ["x_clean", "bw", "ma", "em"],
  "lambda_sup": 1.0, "gamma_sup": 0, "gamma2_sup": 0,
  "checkpoint": "results/02_model/<그룹>/<run>/<run>.pt",
  "fixed_at": "..." }
```

**폴더 정책** — 탐색은 전부 `experiments/` 다. 02 의 여러 런도, 그 런들에 03~06 을 적용해
본 것도 거기 있다. `results/` 는 **완전히 확정한 모델 하나**를 위한 자리이고, 확정 후
`--out-root results` 로 다시 돌려 채운다 (`01_build` 만 예외로 처음부터 `results/`).

**04~07 은 이 파일이 가리키는 모델만 쓴다.** 단계마다 다른 모델을 쓰면 결과가 서로
연결되지 않는다. 재학습은 어디에서도 하지 않는다 — 전부 추론 시점 조작이다.

**split** — 02 는 val 로 탐색했다. 03 부터는 **test** 다. test 는 선정에 쓰이지 않은
기록 9개(1,620분절)이고, 잡음 풀도 train/val 과 다른 구간이다.

---

## 1. 주장의 사슬

네 단계가 하나씩 다음을 떠받친다. 앞이 무너지면 뒤는 의미가 없다.

| # | 주장 | 근거 산출물 | 원고 자리 |
|---|---|---|---|
| ① | 인코더가 배정된 잡음원을 실제로 담는다 | `03_bss/` 대응표 4종 + 배정 대각 | Table 1 · Figure 2 |
| ② | 잡음 인코딩을 끄면 신호가 복원된다 | `04_masked_denoising/` 세 방식 비교 | Table 2 |
| ③ | 복원된 파형이 눈으로도 확인된다 | `05_figure/` 겹침 12장 | Figure 3 |
| ④ | 복원이 **임상 측정값**을 참값에 가깝게 만든다 | `06_ablation/` 형태 지표 오차 | Table 3 · Figure 4 |
| ⑤ | 같은 파이프라인이 외부 데이터에서도 돈다 | `07_validation/` 외부 적용 | Figure 5 (부록) |

①이 성립해야 ②의 "빼기"가 정당하고, ②가 되어야 ③④가 볼 것이 있고, ④까지 되어야
⑤에서 "쓸 수 있다"고 말할 근거가 생긴다.

---

## 2. 03_bss — 성분이 배정대로 담겼는가

**입력** 확정 체크포인트 · test 1,620분절
**출력** `results/03_bss/<run>/test/`

성분 `ŝ_k = D(0,…,z_k,…,0)` 를 뽑아 **실제 주입한 참조 4종**과 대조한다. 배정이 미리
정해져 있으므로 **대각이 주 지표**이고 비대각은 누출 확인용이다.

### 지표 6종

성분과 참조를 각각 분절 내 표준화(평균 0, SD 1)한 뒤 산출한다. 역할이 다르므로
하나로 요약하지 않고 여섯을 그대로 싣는다.

| 지표 | 정의 | 방향 | 무엇을 보나 |
|---|---|---|---|
| \|r\| | 분절 내 Pearson 절댓값 → 분절 간 평균 ± SD (ddof=1) | 높을수록 | 전대역 대응 |
| RMSE_norm | 표준화 신호 차의 RMS | 낮을수록 | 〃 (부호 정렬 안 함) |
| MAD | 그 차의 최댓값 (단위: 표준편차) | 낮을수록 | 최악 지점 |
| r_QRS | 5–15 Hz 통과 후 \|r\| | 높을수록 | **형태**가 맞는가 |
| F1 | 성분에서 검출한 R-피크와 참조 피크 매칭 (허용 50 ms) | 높을수록 | **박동**이 있는가 |
| 대역 에너지 | Welch PSD 의 vlf/lf/qrs/hf 전력 비율 | — | 어느 대역을 점유하는가 |

\|r\| 하나로는 "심장이 없다"(F1 낮음)와 "박동은 있는데 형태가 덜 맞는다"(F1 높고
r_QRS 낮음)가 구분되지 않는다.

**하지 않는 것**: 표본 수(N=3600) 기반 p값(시간적 자기상관으로 독립 관측 가정 불성립) ·
인코더에 참조 이름 붙이기(배정으로 이미 정해져 있다) · 순열 검정 · 코히런스.

### 산출물

| 파일 | 내용 | 원고 |
|---|---|---|
| `assignment_diagonal.csv` | **배정 쌍의 지표 + 최대 누출 참조와 누출비** | **Table 1** |
| `corr_matrix.csv` · `rmse_norm_matrix.csv` · `mad_matrix.csv` | 각 4×4, 평균과 SD 나란히 | 부록 |
| `r_qrs_matrix.csv` · `f1_matrix.csv` · `band_energy.csv` | 확장 지표 3종 | 부록 |
| `metric_agreement.csv` | 세 지표의 행별 지목 일치 여부 | 부록 |
| `figures/correspondence.png` | 대응 히트맵 3종 | **Figure 2a** |
| `figures/s4_extended.png` | \|r\| · r_QRS · F1 · 대역 에너지 4패널 | **Figure 2b** |
| `figures/components_top{1..3}.png` · `overlay_top{1..3}.png` | 상위 3분절 파형 | 부록 |

그림 분절은 **지표 상위 3개**다 — 결과를 보고 고른 사례이므로 제목과 `note.txt` 에
그 사실을 밝힌다.

---

## 3. 04_masked_denoising — 성분 차감 디노이징

**입력** 같은 체크포인트 · test
**출력** `results/04_masked_denoising/<run>/test/`

한 번의 추론으로 세 경로를 모두 계산한다. 추가 학습이 없다.

| 방식 | 계산 | 확인하려는 것 |
|---|---|---|
| **B. 마스킹 재구성** | D(z₁, 0, 0, 0) = ŝ_clean | 잡음 인코딩을 끈 재구성 — **주 노선** |
| A. 성분 차감 | x_noisy − ŝ_bw − ŝ_ma − ŝ_em | 비선형 결합 우회 (비교선) |
| C. 마스킹 디코드 | D(z₁, 0, 0, 0) | B 와 같은 연산임을 확인하는 자리 |
| ⓐ 입력 | x_noisy | 처리 전 |
| ⓑ M0 재구성 | D(z₁, z₂, z₃, z₄) | 마스킹 없이 재구성만 |

**B 를 중심으로 본다.** A 는 함께 싣되 비교선이다.

> **A 를 주 노선에서 뺀 이유.** A 는 원본 `x_noisy` 를 그대로 유지한 채 모델 추정치만
> 뺀다. 모델이 아무것도 못 뽑아도 최소한 `x_noisy` 만큼은 보장되므로, 신호 전체를
> 디코더가 새로 그려야 하는 B 와 출발선이 같지 않다. 처리 전(`x_noisy`)과 나란히 놓고
> "복원이 됐는가"를 물으려면 B 다. A 의 수치는 표에 그대로 남긴다.

> **B와 C는 K=4 에서 같은 연산이다** — 하나만 남기는 것과 나머지 셋을 0으로 치환하는
> 것이 같다. 두 경로를 따로 계산해 최대 절대차를 함께 싣는다(실측 0.000e+00).
> 같다는 것 자체가 확인해야 할 사실이다.

**마스킹의 성격** — 추론 시점의 인코딩 치환이다. 가중치를 다시 학습하지 않는다.
지도학습이라 어느 인코더가 무엇을 담는지 배정으로 정해져 있으므로, "잡음 인코더를 끈다"가
곧 "bw·ma·em 을 뺀다"가 된다. 그 가정이 성립하는지는 03 에서 나온다.

### 지표

`x_clean` 대비 **mV 원단위**(표준화하지 않는다): \|r\| · SSD · RMSE · SNR ·
**ΔSNR vs 입력** · MAD · PRD · CosSim.

ⓐ·ⓑ 를 함께 두는 이유: 개선량 중 어디까지가 재구성 자체의 몫이고 어디부터가 차감의
몫인지 갈라야 한다.

### 산출물

| 파일 | 내용 | 원고 |
|---|---|---|
| `three_ways.csv` | 5행(A·B·C·ⓐ·ⓑ) × 지표 8종 | **Table 2** |
| `three_ways_note.txt` | 정의·B와 C의 실측 절대차 | — |
| `figures/three_ways.png` | 적층 — 다섯 상태를 세로로 | 부록 |
| `figures/three_ways_overlay.png` | x_clean 위에 x_noisy(처리 전)와 B(처리 후)를 겹치고 각각의 잔차 | **Figure 3a** |

---

## 4. 05_figure — 처리 전/후 겹침

**입력** 같은 체크포인트 · test
**출력** `results/05_figure/<run>/test/`

**같은 분절**에 두 칸을 둔다. 두 칸의 y 범위가 같아 잡음이 얼마나 걷혔는지 눈금이 아니라
파형으로 읽힌다.

| 위 | `x_clean` 위에 `x_noisy` — 처리 전 |
| 아래 | 같은 `x_clean` 위에 **B 재구성** — 처리 후 |

### 분절 선정 — 사후 선정임을 밝힌다

B 의 corr 순위로 **상위 4 · 중간 4 · 하위 4**, 모두 12장. 중간 4는 중앙값을 가운데 두고
연속한 4개다. **결과를 보고 고른 사례**이므로 제목에 순위와 백분위를 적는다.
좋은 사례만 싣지 않으려고 하위 4를 함께 낸다.

### 산출물

| 파일 | 내용 | 원고 |
|---|---|---|
| `seg_{상위,중간,하위}{1..4}_<기록>_<분절>.png` | 겹침 12장 | **Figure 3b** (대표 1~2장), 부록(전체) |
| `segments.csv` | 그 12분절의 corr·RMSE·SNR·ΔSNR·주입 SNR | 부록 |

`segments.csv` 는 **06 이 그대로 받는다** — 눈으로 본 사례와 임상 수치를 같은 자리에서
맞춰 보기 위해서다.

---

## 5. 06_ablation — 임상 형태 지표

**입력** 같은 체크포인트 · test (또는 05 가 고른 12분절)
**출력** `results/06_ablation/<run>/test/`

파형이 닮았다는 것과 진단값이 맞는다는 것은 다르다. 여기서는 **임상에서 실제로 재는 값**이
참값에 얼마나 가까운지 본다.

### 5.1 기준점을 공유한다 — 핵심 설계

R-피크는 **x_clean 에서 한 번 검출**해 세 신호(`x_clean` · `x_noisy` · **B 재구성**)에
똑같이 적용한다.

신호마다 따로 검출하면 "박동을 찾았는가"(검출 오차)와 "찾은 자리에서 잰 값이 맞는가"
(측정 오차)가 섞인다. 여기서 보려는 것은 뒤쪽이다 — 기준점을 고정하면 박동이 1:1 로
짝지어져 **박동별 오차**를 낼 수 있고, 남는 차이는 온전히 파형 왜곡에서 온다.
검출 성능 자체는 03 의 F1 에 있다. 분절 가장자리 400 ms 안쪽 박동은 창이 잘려 뺀다.

### 5.2 지표 셋

**① ST 분절 편위 — ST60 · ST80 (mV)**
QRS 가 끝나는 지점(J점)에서 60 ms, 80 ms 뒤의 신호 높이를 기저선(PQ 구간) 대비로 잰
값이다. 위로 올라가면 ST 상승, 아래로 내려가면 ST 하강이라 부르고, **심근경색과 허혈
진단의 핵심 지표**다.

```
기저선 = mean(x[R−80ms : R−40ms])      PR 구간, 등전위선
J점    = R + 40ms                       (QRS 종료점 검출은 잡음에서 특히 불안정)
ST60   = x[J+60ms] − 기저선,   ST80 = x[J+80ms] − 기저선
```

기저선 대비로 재므로 기저선이 흔들리면 그대로 틀린다. **bw 가 정확히 그 기저선을 흔든다** —
이 프레임워크가 가장 직접적으로 겨냥하는 지표다.

**② QT 간격 — QT · QTc (ms)**
Q파 시작부터 T파 끝까지의 시간이다. 심실이 수축하고 회복하는 데 걸리는 시간이고,
**길어지면 부정맥 위험**이 있다. QTc 는 심박수 보정값(Bazett: QT / √RR).
neurokit2 `ecg_delineate`(dwt)로 QRS 시작점과 T 종료점을 찾는다.

T파는 진폭이 작고 완만해 잡음에 묻힌다. 다만 **T 종료점 검출 자체가 까다로워 참값도
흔들린다** — 참값에서 안 잡힌 박동은 세 계열 모두에서 뺀다. 그래서 이 지표는 다른 둘과
성격이 다르고, 원고에서도 그 한계를 함께 적는다.

**③ QRS 진폭·면적 — R진폭 (mV) · QRS면적 (mV·ms)**
R파의 높이와 QRS 복합체 아래 면적. 둘 다 **심실 비대 판정** 등에 쓰인다.

```
R진폭   = max(x[R±50ms]) − 기저선
QRS면적 = Σ|x[R−50ms : R+60ms] − 기저선| / fs × 1000
```

진폭이 곧 지표라 잡음이 그대로 오차가 된다. 계산이 단순한 것이 장점이다.

### 5.3 읽는 법

**표와 그림의 값은 모두 오차다. 작아졌으면 참값에 더 가까워진 것이다.**

```
x_clean 에서 잰 ST60  = 0.05 mV   (참값)
x_noisy 에서 잰 ST60  = 0.18 mV   → 오차 |0.18 − 0.05| = 0.13 mV
B 재구성에서 잰 ST60   = 0.11 mV   → 오차 |0.11 − 0.05| = 0.06 mV
```

`개선된_박동비율` 은 박동 단위로 처리 후 절대오차가 처리 전보다 작아진 비율이다.
부호를 살린 **편향**도 함께 싣는다 — 오차가 한쪽으로 치우치는지 보기 위해서다.

### 5.4 산출물

| 파일 | 내용 | 원고 |
|---|---|---|
| `metric_summary.csv` | 지표 6종 × (참값·처리 전·처리 후) 값·편향·절대오차 중앙/평균/p90·개선 비율 | **Table 3** |
| `beats.csv` | 박동별 원값과 오차 (기록·분절·박동 위치) | 부록 |
| `error_by_record.csv` | 기록별 절대오차 중앙값, 처리 전/후 나란히 | 부록 |
| `figures/error_compare.png` | 위: 박동 전체 오차 분포 상자그림 / 아래: 기록별 중앙값 변화 | **Figure 4** |

---

## 6. 07_validation — 외부 데이터

**입력** 같은 체크포인트 · 외부 소스
**출력** `results/07_validation/<run>/<source>/`

확정 체크포인트를 **재학습 없이** 외부 ECG 에 적용한다. 학습·마스킹·복원 경로를 하나도
바꾸지 않는다. 지도학습이라 배정이 정해져 있으므로 **외부에서도 B 재구성을 그대로
만들 수 있다** — 자기지도 갈래에서는 어느 인코더가 잡음인지 사후에 정해야 했던 부분이다.

| 소스 | 신호 | 원 규격 | → 360 Hz |
|---|---|---|---|
| vitaldb | SNUADC/ECG_II | 500 Hz, mV | 18/25 정확 |
| mimic_iv | 유도 II | 249.89 Hz, mV | 36/25 (오차 0.045%) |
| galaxyppg | Polar H10 ECG | 130 Hz, µV | 36/13 정확 |

**분절 선정은 사전 규칙**이다 — 지표를 보지 않는다. 기록 앞 5분 · NaN · 평탄비 > 0.05 ·
포화비 > 0.005 · R-피크 < 5 · 심박수 30–200 bpm 밖 · RR 변동계수 > 0.5 를 버리고, 남은 것
중 각 기록 앞에서부터 순서대로 취한다.

**진폭 정합** — 모델은 mV 원값을 그대로 받도록 학습됐고 정규화 단계가 없다. 그 전제는
MIT-BIH 안에서만 성립하므로 분절마다 상수배해 학습 입력 SD 중앙값에 맞춘다. 배율은
`segments.csv` 에 남는다 (`--no-scale` 로 끌 수 있다).

> **성능은 주장할 수 없다.** 참값이 없으므로 "얼마나 깨끗해졌나"를 잴 방법이 없다.
> NSTDB 잡음 조각을 참조로 붙이는 방식도 쓰지 않는다 — 외부 데이터에 실제로 섞인 잡음과
> 무관한 파형이라 상관값이 "대역이 비슷하다"만 반영하고 파형 일치를 뜻하지 않는다.
>
> 낼 수 있는 것은 ① 성분의 주파수 성격(중심주파수·대역 전력비) ② B 가 입력과 얼마나
> 다른가 ③ 파형이 눈으로 어떻게 보이는가 뿐이다.

### 산출물

| 파일 | 내용 | 원고 |
|---|---|---|
| `restore.csv` | A·M0 가 입력과 얼마나 다른가 (\|r\|·RMSE·SD비) | 부록 |
| `spectrum.csv` | 성분별 중심주파수와 대역 전력비 (+ 입력·M0) | 부록 |
| `corr_matrix.csv` 외 2종 | 성분 × (입력·M0·A) 지표 3종 | 부록 |
| `segments.csv` · `note.txt` | 사용 분절과 정합 배율, 선정 규칙 | 부록 |
| `figures/overlay_top{1..10}.png` | 입력 위에 A 겹침 + 빠져나간 양 | **Figure 5** |
| `figures/components_top{1..10}.png` · `spectrum.png` | 성분 파형·PSD | 부록 |

---

## 7. 원고 배치 요약

| 요소 | 내용 | 출처 |
|---|---|---|
| Table 1 | 배정 쌍의 성분 정렬 지표와 누출 | `03_bss/.../assignment_diagonal.csv` |
| Table 2 | 복원 세 방식 + 기준선 2종 | `04_.../three_ways.csv` |
| Table 3 | 임상 형태 지표 오차 (처리 전/후) | `06_.../metric_summary.csv` |
| Figure 1 | 파이프라인 도식 + 인코더-소스 배정 | (작성 예정) |
| Figure 2 | 대응 히트맵 · 확장 지표 4패널 | `03_bss/.../figures/` |
| Figure 3 | 복원 겹침 (전체 + 대표 분절) | `04_.../three_ways_overlay.png`, `05_.../seg_*.png` |
| Figure 4 | 임상 지표 오차 분포와 기록별 변화 | `06_.../figures/error_compare.png` |
| Figure 5 | 외부 데이터 적용 | `07_.../figures/overlay_top*.png` |
| 부록 | 02 여덟 런 탐색 결과 · 나머지 표 전부 | README §2.1.1, 각 폴더 |

---

## 8. 사전 등록 사항

결과를 보기 **전에** 확정한 것들이다. 결과를 보고 바꾸면 그 사실을 명시한다.

1. **체크포인트 선택** — `argmin L_sup^val`, 후보는 `≤ 1.5 × min` 인 판정 에폭. 민감도 {1.2, 1.5, 2.0}.
2. **조기 종료** — `val_sup` 50에폭 정체.
3. **인코더-소스 배정** — enc1 clean · enc2 bw · enc3 ma · enc4 em. 사후에 바꾸지 않는다.
4. **split** — 02 는 val, 03~06 은 test. test 는 선정에 쓰지 않는다.
5. **06 의 기준점** — x_clean 에서 검출해 세 신호에 공유.
6. **06 의 측정 창** — 기저선 [R−80, R−40]ms · J점 R+40ms · ST 60/80ms · QRS [−50, +60]ms · R진폭 ±50ms.
7. **07 의 분절 선정** — 위 §6 의 사전 규칙. 지표를 보지 않는다.
8. **사후 선정임을 밝히는 곳** — 03 의 상위 3분절, 05 의 12분절. 제목과 note 에 명시.
