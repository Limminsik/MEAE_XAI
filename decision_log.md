# decision_log.md — 설계 결정 대장

원고 방법·고찰 집필과 심사 답변에 바로 인용하기 위한 대장. **한 줄 = 한 결정.**
근거의 상세 수치는 `results/data_notes.md`의 해당 절을 참조한다.
설계서 본문(`RESEARCH_DESIGN.md`)과 어긋나는 항목이 생기면 이 대장이 아니라 설계서를 고친다.

날짜는 전부 2026-08-25 (T1–T5 세션).

| # | 결정 | 근거·증거 | 반영 위치 |
|---|---|---|---|
| D01 | Python 3.9.21 + uv + torch cu129 고정, `neurokit2==0.2.10` 핀 | 최신 neurokit2(0.2.12)는 `float \| None` 문법이라 py3.9에서 import 실패 | `requirements.txt`, 설계 §1 |
| D02 | NSTDB 잡음은 **채널 0(noise1)** 사용 | 잡음도 2채널 홀터로 녹음돼 noise1/noise2를 가짐. `nst` 도구가 채널 번호끼리 짝지어 섞는 관례를 따름 (118e00−118 역산에서 ch0↔noise1, ch1↔noise2가 **동일 지연 531200**에 수렴) | `configs: data.noise_channel: 0`, notes §2 |
| D03 | 기록 단위 분할 **32/5/9**, 층화 없음, `split_seed: 42` 고정 | 46 × (0.7/0.1/0.2) = 32.2/4.6/9.2로 정수가 안 떨어짐. split.json 1회 생성 후 모든 K·시드가 공유하며 학습 시드는 분할에 무관 | `src/data/split.py`, `data/processed/split.json`, notes §1 |
| D04 | 주입 SNR의 신호 전력을 `mean(x²)` → **`var(x)`** 로 변경 | MIT-BIH 전극 오프셋(DC)이 `mean(x²)`의 **중앙 35.1%·최대 97.5%** 를 차지해 명목 SNR과 실제 SNR이 분절마다 0–10 dB 어긋남 | 설계 §4-4, `src/data/build.py`, notes §2 |
| D05 | 실측 SNR 검증도 동일하게 분산 기준 | 주입과 검증의 정의가 다르면 테스트가 항상 실패 | 설계 §4 DoD·§11, `tests/test_s1.py` |
| D06 | 비트 심볼에 **`f`(paced+normal 융합박) 추가** | 제외하면 217번 기록의 실제 심박 260개가 R-피크에서 누락 | 설계 §4-2, `src/data/build.py`, notes §7 |
| D07 | 잡음 강도 범위 **[0, 12] dB 확정** | 주입 후 RMS가 NSTDB 원본 범위 안(배율 중앙 0.23–0.96×). 단 ma는 47.3%에서 증폭되며 95%분위 3.5배 | `configs: data.noise_snr_range_db`, notes §3 |
| D08 | `nstdbgen`(nst 도구) 재사용 **기각** | nst는 잡음 1종만·구간의 43%만 주입하고 SNR이 6단계 고정이며 잡음 원본 시간 분할을 통제할 수 없음 | notes §5-3, 설계 §4 |
| D09 | MIT-BIH 자체 잡음 구간을 **학습에 쓰지 않음** | 전체의 1.68%(139분절)뿐이고 clean·bw/ma/em 참조가 없어 S4 채점이 불가능 | notes §5-2 |
| D10 | 선행 저장소 2개를 **무수정 이식**(모델·손실 파일만) | MIT 라이선스. 커밋 `d0c94a9d`(기반), `91f1e0e2`(후속) | `src/model/_vendor_*.py`, notes §T3 |
| D11 | sparse mixing은 **`WeightSeparationLossAlternative`** 채택 | ECG 실험 설정(`mesa_ecg_bss`)이 쓴 구현. PPG 논문은 `WeightSeparationLoss`+`sep_lr 0.05`로 짝이 다름 | `configs: loss.sep_impl`, `src/model/losses.py`, notes §8 |
| D12 | 입력을 **3840으로 대칭 제로 패딩**(양쪽 120) | 선행 인코더 깊이가 **8**(MaxPool 2배×8 = 256배)이라 입력이 256의 배수여야 함. 3600→3584로 어긋남. 후속 저장소도 6000→6144(=256×24)로 패딩 | 설계 §5·§5A, `configs: data.pad_to/pad_each`, `src/model/meae.py`, notes §9 |
| D13 | 4096이 아니라 3840 | 필요 조건은 2의 거듭제곱이 아니라 **256의 배수**. 3840이 패딩 240으로 절반이라 경계 왜곡 구간이 작음 | 설계 §5A, notes §9 |
| D14 | 모든 상관·지표는 **crop 후 중앙 3600**에서 계산 | 패딩 0 구간이 상관을 희석. `metrics.score()`가 3840 입력을 `ValueError`로 차단 | `src/model/meae.py`, `src/metrics.py`, `tests/test_metrics.py` |
| D15 | **K = {4, 8}** (6 제외) | K=6은 디코더 `GroupNorm(num_groups=K)`가 채널 32/64/128/256을 나누지 못해 `ValueError`. 채널 변경은 §0 원칙 2 위반 | 설계 §3·§5, `src/model/meae.py`, notes §10 |
| D16 | 재구성 손실 **BCE → MSE**, zero-recon도 함께 MSE | 정규화를 하지 않고 ECG는 음수를 가짐. 선행은 min-max [0,1] + BCEWithLogits | 설계 §5, `src/model/losses.py`, notes §11 |
| D17 | 학습 설정은 선행을 따르지 않음 (`max_epoch 100` + 조기종료, 체크포인트는 분리 품질) | 선행은 `max_epochs 25`, `monitor: recon_loss/train`. 선행이 수동으로 하던 체크포인트 고르기를 자동화한 것이 우리 기여 | 설계 §6, `configs: train.*`, notes §11 |
| D18 | **S5를 "비교군 성능 평가" → "마스킹 전후 = 판별의 인과 검증"으로 재정의.** DAE·대역통과·웨이블릿 전부 삭제 | 주장의 구조가 ①분해→②판별→③판별이 진짜다이며, ③의 대조는 외부 기법이 아니라 같은 모델에서 마스킹만 바꾼 조건 | 설계 §8·§10·§12, notes §14 |
| D19 | 인코더 단독 마스킹 아블레이션을 **S5의 주 실험으로 승격** | S4는 상관(연관) 근거, 아블레이션은 개입(intervention) 근거로 논증 성격이 다름 | 설계 §8, notes §14 |
| D20 | **지표 위계**: SNR 개선=주, RMSE=교차확인, F1=경고등, SDNN=판정 보류 | R-피크 F1 상한이 **0.981**(검출기 vs 사람 주석), 마스킹 전 0.956 → 개선 여지 2.5%p뿐이라 개선의 증명에 쓸 수 없음 | 설계 §8, notes §13 |
| D21 | **체크포인트 지표 교체**: `분리 품질 = 잡음별 max\|r\| 평균 − 0.15×(최고 인코더 중복 수)` | 초판 정의는 붕괴 상태(에폭 5, 네 참조 모두 같은 인코더 1위, 재구성에 R-피크 없음)를 최고로 뽑음. 페널티는 0.05–0.30 전 구간에서 같은 에폭 선택 | 설계 §6, `configs: train.separation_duplicate_penalty`, `src/train.py`, notes §16 |
| D22 | **헝가리안 1:1 강제 배정 기각** | 참조를 서로 다른 인코더에 강제 배정하면 분리 실패 시에도 그럴듯한 점수가 나와 **실패를 가림** | 설계 §6, notes §16 |
| D23 | **λ_mixing 1e-3 → 1e-2 상향** (파일럿 검증) | λ=1e-3에서 mixing 원값이 8.3%만 감소(재구성은 91.6% 감소), 디코더 그래디언트 비 **0.0002**. 1e-2에서 59.8% 감소·재구성 손해 없음·clean\|r\| 0.435→0.555 | `--lambda-mixing` 오버라이드, notes §17 · **본 설정 반영은 미확정(OPEN-01)** |
| D24 | **판정 기준 선등록** — λ 1e-2·1e-1 모두에서 bw·ma 최고 인코더가 같으면 결과로 확정하고 판별 단위를 심장/잡음군으로 조정 검토 | 결과를 보고 기준을 옮기면 선택 편향. 실행 **전에** 기록 | notes §18 |

## 아직 결정되지 않은 것

`OPEN_ITEMS.md` 참조. 특히 λ 최종값(OPEN-01), δ 결정 절차(OPEN-02), 판별 단위(OPEN-03)는
설계서 §6·§7과 직결되므로 확정 전까지 해당 조항을 수정하지 않는다.

## T5.6 – T6.5 추가 결정 (2026-08-25)

| # | 결정 | 근거·증거 | 반영 위치 |
|---|---|---|---|
| D25 | **S4 산출물 패키지 v1 확정** — ①상관 행렬 ②참조 간 상관 ③**기여 분해** ④시각 ⑤통계 | 상관은 "모양이 닮았나"만 답한다. 기여 분해(다중 회귀 설명 분산 몫)가 **"에너지의 몇 %를 설명하나"** 에 답한다. 파일럿에서 enc3가 clean 36.07% 대 잡음 0.12–0.20%로 **양적 배타성** 확인 | 설계 §7, `src/s4_identify.py`, `results/PILOT_SUMMARY.md` |
| D26 | **위계 확정**: 재구성 = 목표 · 분리 = 부산물 · 판별 = 우리 기여 | 재구성이 부실한 모델의 성분은 해석 대상이 아니다. 이 위계가 T6.5 관문을 재구성 단일 축으로 구성한 근거이며, **원고 방법 절 서두 문장**에 반영한다 | 설계 §6.5, `src/fidelity.py`, MANUSCRIPT_PENDING |
| D27 | **T6.5 재구성 충실도 관문 신설** — ①전대역·15–40 Hz 보존율 ≥ 0.7 ②디노이징 지수 `RMSE(x̂,x_noisy) < RMSE(x̂,clean)` | 육안으로 고주파 잔떨림 소실이 확인됐고, 파일럿 실측에서 15–40 Hz 보존율 0.353. 전대역만 보면 0.872로 통과해버린다(K8_seed42는 전대역 1.201인데 15–40 Hz 0.326) | 설계 §6.5, `src/fidelity.py` |
| D28 | 잔차 상관·R-피크 진폭비를 **관문에서 진단으로 강등** | 관문에 두면 "잔차–ma가 낮으니 통과"로 오판한다. 실제로는 잔차–clean 0.16–0.31 ≫ 잔차–잡음 0.04–0.20 — 못 담는 것이 **QRS 모서리**임을 이 창으로 밝혔다 | 설계 §6.5, `results/t6_5_diagnostics.csv` |
| D29 | **체크포인트 지표 수정 2회차(마지막)**: 잡음 3종 → **참조 4종(clean 포함)** 최대 \|r\| 평균 − 0.15×중복 | 1회차(중복 페널티)는 "세 잡음이 한 인코더로 몰리는" 붕괴만 잡았다. clean 항이 있어야 심장 성분을 실제로 담은 모델이 이긴다. **두 번 모두 val 기반이며 test는 봉인 상태다** | `configs: train.*`, `src/train.py`, 설계 §6 |
| D30 | **체크포인트 후보 게이트 (처방 A)** — `val_recon ≤ 1.2 × 최소` 구간에서만 선택 | D29로도 2/6이 에폭 13(184에폭 중)을 골랐다. 게이트 적용 후 41·50으로 이동, 조기 붕괴 선택 **2/6 → 0/6**. 15–40 Hz 보존율 하한도 0.000 → 0.309 | 설계 §6, `configs: train.checkpoint_recon_gate` |
| D31 | **후보 가중치 상위 3개 보관** | 반복 재실행의 근본 원인은 가중치 미보관이었다. 이제 지표를 다시 손봐도 **재학습 없이 재선택** 가능 | `src/train.py: CheckpointPool`, `logs/<run>/checkpoints.json` |
| D32 | `patience` 10→30, `max_epoch` 100→300 | 조기 종료가 미수렴 시점에서 끊고 있었다. 연장 후 K4가 100·144에폭까지 학습됨. **D30과 독립된 수정이다** | `configs: train.*` |
| D33 | **`lambda_z_l2` 1e-2 → 1e-3 (선등록 처방)** | 처방 A 적용 후에도 15–40 Hz 보존율 0.309–0.683으로 6/6 미달. 인코딩 크기를 누르는 항이 고주파 표현 여력을 제한한다는 진단. **기준 0.7은 이동하지 않았다** (0.683도 미달 처리) | `configs: loss.lambda_z_l2` |
| D34 | **K 역할 확정**: K=8 = 주 분석(판별·마스킹·아블레이션·외부 시연), K=4 = 강건성 비교 + 시각 사례 | 아블레이션 해상도, 전수 지도의 마스킹 계조, 선행 기준선(8), T6 조기 선택 빈도 | 설계 §5·§7, OPEN_ITEMS K 항목 종결 |
| D35 | **마스킹 조건 사다리 M0–M5를 정식 실험으로** | 단일 규칙을 변호하는 대신 약→강 개입의 계단 전체를 싣는다. M5(clean 인코더 마스킹)는 음성 대조 | 설계 §8 |
| D36 | **M2·M3 선정 기준을 val에서 정의하고 test에 적용** — 잡음 기여 합 ≥ 10% | 기여 분해 기준. 결과를 보고 임계를 옮기지 않는다 | 설계 §8 |
| D37 | **개선 사이클 종료 선언** — `lambda_z_l2` 재실행이 마지막 개선 시도 | 합격·미달과 무관하게 T7로 진행한다. 미달 시 도달 수치를 원고 한계 절로 서술하고, 3차 처방(`base_ch` 확대)은 **저널 확장 과제**로만 기록 | OPEN_ITEMS, `results/data_notes.md` |

## 원고 결과 절 구성 변경 (2026-08-25, T6.9 이전 선등록)

**아래 D38–D41은 리허설 산출물을 만들기 전에 기록한다.** 규칙을 먼저 박고 적용한다.

| # | 결정 | 근거 | 반영 위치 |
|---|---|---|---|
| D38 | **메인 결과 = 대표 모델 1개의 인코더↔실제 잡음 대응.** 6회 반복 통계가 아니다 | 이 연구의 주장은 "판별된다"이지 "여러 번 해도 비슷하다"가 아니다. 반복 통계는 재현성 근거일 뿐 결과의 본체가 아니며, 앞세우면 판별이라는 기여가 표 뒤로 밀린다 | 설계 §7·§10, MANUSCRIPT_PENDING |
| D39 | **대표 모델 선정 규칙 (사전 등록)**: **K=8 중 검증셋 분리 품질(§6 정의) 최고 모델** | K=8은 주 분석 축(D34). 분리 품질은 체크포인트 선택과 같은 잣대라 일관된다. **결과를 보고 고르지 않기 위해 적용 전에 기록한다** | 설계 §7, `src/rehearsal.py` |
| D40 | **표 1 = 인코더 × 참조 대응표, 각 칸에 `r`과 `RMSE` 병기.** RMSE는 **원값·배율 보정 두 버전**을 리허설에서 산출해 비교한 뒤 게재본을 정한다 | 상관은 모양만, RMSE는 크기까지 말한다. 비선형 디코더에서 성분의 절대 크기는 임의 스케일이므로 배율 보정본이 필요하나, 원값도 함께 봐야 보정의 효과를 판단할 수 있다 | 설계 §7, `src/rehearsal.py` |
| D41 | **기여 분해는 표 1에서 빼고 본문 서술 또는 보조 표로 강등.** 시드 재현성·K 비교도 결과 절 말미 1–2문장 또는 보조 자료로 강등 | 표 1에 세 종류 수치(r·RMSE·기여%)를 다 넣으면 읽히지 않는다. 기여 분해는 M2·M3 선정 기준으로 계속 쓰이므로 산출은 유지하되 게재 위치만 내린다 | 설계 §7·§10 |
| D42 | **대표 분절 선정 규칙 (사전 등록)**: 각 참조의 매칭 인코더 \|r\|의 **평균 백분위가 중앙(50%)에 가장 가까운 분절**. 최고·최저 사례가 아니라 전형 사례를 싣는다 | 최고 사례만 싣는 것은 선택 편향. high/mid/low 3종은 보조 자료로 유지 | `src/rehearsal.py` |

## 규칙 충돌 해소 — 대표 모델 선정 개정 (2026-08-25)

| # | 결정 | 근거 | 반영 위치 |
|---|---|---|---|
| **D43** | **D39 개정**: 대표 모델 = ~~K=8 중~~ → **T6.5 충실도 관문을 통과한 모델 중** 검증 분리 품질 최고 | **위계 일관성.** "재구성이 미달인 모델의 성분은 해석 대상이 아니다"는 D30·D26의 논리를 체크포인트 선택뿐 아니라 **모델 선정에도 일관 적용**한다. D39(K=8 우선)와 T6.5 판정이 충돌했고 — K=8 중 분리 품질 최고인 K8_seed202의 보존율이 0.629로 미달 — 위계상 충실도가 앞선다. **현재 통과 모델이 1개(K4_seed2026)뿐이므로 선택의 자의성이 없다.** 결과를 보고 고른 것이 아니라 이미 확정된 관문 판정을 따른 것이다 | 설계 §7, `src/rehearsal.py` |
| **D44** | **D34 조정**: K=8 = ~~주 분석~~ → **아블레이션 해상도 비교 및 강건성 제시**. 주 결과는 **K=4 합격 모델 기준** | D43의 귀결. K 역할은 결과가 아니라 충실도 관문이 정한다 | 설계 §5·§7·§8 |
| D45 | 표 1 각주에 **대표 모델의 충실도 수치를 병기** (보존율 0.843 · R-피크 진폭비 1.015 · 잔차–clean 0.048) | 대표 모델이 왜 그 모델인지를 표 안에서 바로 확인할 수 있어야 한다 | `src/rehearsal.py` |
| D46 | **원고 한계 절 추가**: 6개 모델 중 1개만 충실도 기준을 충족했으며 주 결과는 해당 모델 기준이다. 나머지 5개의 판별 구조 재현성은 보조 자료로 제시한다 | 통과율을 숨기지 않는다. 재현성은 별도로 보이되 주 결과와 섞지 않는다 | MANUSCRIPT_PENDING, `results/data_notes.md` |

## D43·D44 보류 (2026-08-25)

| # | 결정 | 근거 |
|---|---|---|
| D47 | **D43(대표 모델 = 충실도 통과 모델 중 최고)·D44(K 역할 조정)를 보류.** 진단 실험 결과에 따라 재결정한다 | D43은 "충실도 통과 모델이 K=4에만 있었다"는 **임시 타협**이었다. 주 분석 축은 K=8로 확정돼 있고(아블레이션 해상도, M0–M5 마스킹 사다리의 계조), 지금은 재구성 병목 자체를 규명하는 단계이므로 **실제 사용할 축인 K=8에서 진단**해야 한다. 진단 결과가 나오면 대표 모델 규칙을 다시 정한다 |
| D48 | **λ_z 병목 진단 실험 (1회, 단일 변수)** — K=8, seed 42, `lambda_z_l2 = 0`. 나머지 전부 동결(λ_mixing 1e-2, patience 30, max_epoch 300, 체크포인트 후보 게이트). **사전 등록 판정 기준 3종**: ①보존율 크게 개선(0.9 접근) + 분리 유지 → L2가 병목, 0~1e-3 절충값 탐색 ②개선 미미 → L2는 병목 아님, 구조·용량(base_ch 32→64)으로 직행 ③보존율 개선 + 분리 붕괴(clean 전담 인코더 소멸, 인코더 간 성분 상관 급증) → **재구성–분리 상충의 실증**, 그 자체가 논문에 실을 발견이며 절충점 탐색으로 전환 | 추측을 끝내기 위한 결정 실험. 선행은 Lz를 sparse mixing·zero reconstruction과 함께 "추론 시 분리 품질을 보장하는" 정규화 3종으로 묶어 제시하므로, **L2 제거가 분리에 영향을 줄 가능성을 열어두고 판정**한다 |
