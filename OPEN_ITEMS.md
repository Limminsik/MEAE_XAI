# OPEN_ITEMS.md — 미결 사항 보드

닫힌 결정은 `decision_log.md`로 옮긴다. 각 항목은 **차단 요인**과 **다음 행동**을 반드시 갖는다.
갱신 기준일: 2026-08-25 (T5 파일럿 2회 완료 시점).

---

## OPEN-01 · λ_mixing 최종값 확정
- **상태**: 진행 중. λ=1e-3, 1e-2 완료. **1e-1 미실행.**
- **현재 근거**: 1e-2가 1e-3보다 전면 우세 — mixing 원값 59.8%↓(vs 8.3%↓), 검증 재구성 0.0299(vs 0.0318), clean 최대 |r| 0.555(vs 0.435), 체크포인트 에폭 45(vs 붕괴 시점 5).
- **차단 요인**: 없음. 즉시 실행 가능(4.7분).
- **다음 행동**: `python -m src.train --k 8 --seed 42 --lambda-mixing 1e-1` 실행 → 3조건 비교표 → 최종값을 `configs/default.yaml: loss.lambda_mixing`에 반영.
- **연결**: OPEN-03(선등록 기준 판정이 이 실행에 달려 있음), `decision_log` D23.

## OPEN-02 · δ(1위−2위 격차 임계) 결정 절차
- **상태**: 미착수. 초기값 0.1이 그대로 남아 있음.
- **현재 근거**: λ=1e-2 최고 체크포인트에서 bw+ma 인코더(enc2)의 격차가 **0.096** — δ=0.1에 4/1000 차이로 탈락.
- **차단 요인**: **결과를 보고 δ를 내리면 기준을 옮기는 것이 된다.** 절차를 먼저 확정해야 함.
- **다음 행동**: δ를 **검증셋에서만**, 결과 독립적인 규칙으로 정하는 절차를 선등록. 후보 —
  (a) 검증셋 전체 인코더의 격차 분포에서 상위 사분위 등 분포 기반 임계,
  (b) 무작위 순열(성분–참조 짝 섞기)로 만든 귀무분포의 95%분위,
  (c) 0.1 고정 유지.
  (b)가 통계적으로 가장 방어 가능. **사용자 승인 필요.**
- **연결**: 설계 §7 판정 규칙, OPEN-03.

## OPEN-03 · 판별 단위 확정 (잡음 3종 vs 심장/잡음군)
- **상태**: 선등록 기준 대기. λ=1e-2에서 **bw·ma가 63/63 에폭 전부 같은 인코더 공유**.
- **현재 근거**: λ=1e-2 에폭 45 — 신호 인코더 1개(enc3, 격차 0.542), em 인코더 1개(enc5, 격차 0.219), bw+ma 융합 1개(enc2, 격차 0.096), 나머지 5개 혼재. **Gate G1 조건1 미충족.**
- **차단 요인**: OPEN-01(λ=1e-1 결과).
- **다음 행동**: λ=1e-1 결과 확인 → 선등록 기준(`data_notes` §18)대로 판정 → 3분류(심장 / em / bw+ma군)로 조정하는 설계 변경안 작성 → 승인 후 §7 Gate G1 개정.
- **연결**: 설계 §7 Gate G1(현재 조항과 정면 충돌 — 감사 항목 C25), `decision_log` D24.

## OPEN-04 · SDNN 채택/제거 판정
- **상태**: 미착수. 지표 위계에서 "판정 보류"로 남아 있음.
- **차단 요인**: 없음. 파일럿 데이터로 지금 실측 가능.
- **다음 행동**: test 분절에서 clean 기준 SDNN의 **상한(검출기 vs 주석)과 분산**을 F1과 같은 방식으로 실측 → 개선 여지가 측정 오차보다 큰지 판정 → 채택/제거를 `decision_log`에 기록.
- **연결**: 설계 §8 지표 위계, `data_notes` §13.

## OPEN-05 · 잔차 분해 — **정의 확정 (2026-08-25)**

두 갈래를 모두 채택한다. **성분 크기는 신뢰하지 않고 모양(상관)으로 판별한 뒤,
크기는 최소제곱 배율로 보정하고 해부한다** — 비선형 디코더에서 성분의 절대 크기는
임의 스케일이기 때문이다.

### (d) S4 해석용 — 성분별 배율 보정 잔차 **(주 목적)**
확정된 인코더 `k`와 그 대응 참조 `r`에 대해

```
β  = argmin ‖r − β·x̂_k‖²          = ⟨r, x̂_k⟩ / ‖x̂_k‖²
설명 에너지 비율 = 1 − ‖r − β·x̂_k‖² / ‖r‖²
```

를 구하고, **설명 에너지 비율**과 **잔차 `r − β·x̂_k`의 스펙트럼**을 보고한다.
답하려는 질문: *"enc2가 bw+ma 에너지의 몇 %를 설명하고, 못 잡은 것은 어느 대역인가."*
상관(모양 일치)만으로는 "얼마나 담고 있나"에 답할 수 없으므로 판별 결과의 해부에 필요하다.

- 산출: `residual_decomposition.csv` (인코더×참조별 β, 설명 비율), 잔차 스펙트럼 그림
- 차단 요인: OPEN-03(어느 인코더에 어느 참조를 대응시킬지가 판별 단위에 달림)

### (b) S5 보강 — 마스킹 후 잔차의 참조 분해
복원 신호와 clean의 잔차를 bw·ma·em에 **다중 회귀**하여 어느 잡음이 얼마나 남았는지 정량화한다.

```
(x_masked − x_clean) ≈ β_bw·bw + β_ma·ma + β_em·em + ε
```

아블레이션의 방향성 검증을 **잡음 유형 수준으로 세분화**한다 — "bw 인코더를 껐더니
잔차에서 bw 기여만 줄었는가"를 직접 확인할 수 있어 개입 근거가 한 단계 강해진다.

- 산출: `ablation_residual.csv` (아블레이션 조건 × 잡음별 잔여 기여)
- 차단 요인: OPEN-06(본 학습), OPEN-03

**다음 행동**: `src/residual.py` 신설. OPEN-03 확정 후 구현하며, 설계서에 §8.1로 편입한다.

## OPEN-06 · 본 학습 6회
- **상태**: 미착수. 파일럿 2회는 λ가 달라 본 결과로 쓸 수 없다.
- **차단 요인**: OPEN-01(λ 확정).
- **다음 행동**: λ 확정 후 K {4, 8} × 시드 {42, 202, 2026} = **6회 전부 새로 실행**. 에폭당 4.5초 × 약 60에폭 × 6회 ≈ **30분**.
- **주의**: 설계 §6은 "본 학습 5회", §12는 "본 학습 8회"로 서로 다르다(감사 항목 C20·C38). 파일럿을 본 결과에 포함하지 않으므로 **6회가 맞다.**

## OPEN-07 · S6 외부 데이터 준비
- **상태**: 부분 완료. GalaxyPPG 압축 해제본 보유. **MIMIC-III Waveform 미보유**(`data/raw/mimic3wdb` 비어 있음).
- **차단 요인**: MIMIC-III는 PhysioNet 자격 인증(CITI 교육 + 데이터 사용 협약) 필요 가능성 — 확인 안 됨.
- **다음 행동**: (1) MIMIC-III Waveform Database Matched Subset의 접근 요건 확인(공개 vs 자격 필요), (2) GalaxyPPG Polar H10 ECG의 **단위가 mV인지 raw ADC인지** 확인, (3) `test_resample` 구현(125→360, 130→360).
- **보류 중 제안**: MIT-BIH 자체 잡음 구간(139분절)을 S6 시연에 **추가**하는 안. 같은 장비·유도·환자군이라 도메인 변수가 통제되고 R-피크 주석이 있어 정량 지표를 하나 건질 수 있다. **미승인 상태.**

## OPEN-09 · 성분 중복 경보 임계 재보정
- **상태**: 재보정 대기. 설계 §6 붕괴 규칙은 `인코더 쌍 |corr| > 0.7이 5에폭 지속`이다.
- **현재 근거**: 파일럿에서 정상 학습 중에도 `max_pair_corr`가 **0.32–0.67**로 움직여
  임계 0.7이 상시 문턱에 걸린다. 경보가 항상 울리면 경보로서 기능하지 못한다.
- **차단 요인**: OPEN-01. λ 최종값에서 분포가 달라질 수 있다(λ가 클수록 혼합이 억제되므로
  성분 간 상관도 낮아질 것으로 예상).
- **다음 행동**: λ=1e-1 실행 후 **정상 학습 구간의 `max_pair_corr` 분포**(3조건 × 60에폭)를
  보고 임계를 다시 놓는다. 후보: 분포의 95%분위, 또는 K별로 다른 값.

## OPEN-10 · C등급 문서화 채무 일괄 처리
- **상태**: 대기. T5.5 감사에서 C1–C9로 목록화됨(C6은 즉시 처리 완료).
- **내용**: §2 구조에 신규 파일 반영, §4 meta 필드, §4 잡음 채널 조항, §5A T3 결과 본문 편입,
  §7 코드 예시 외 잔여, §11 실제 테스트 15종 반영, §13 설정 해시 미구현.
- **차단 요인**: OPEN-01·OPEN-03. 판별 단위가 바뀌면 §7·§11이 또 바뀐다.
- **다음 행동**: OPEN-03 확정과 동시에 일괄 반영.

## OPEN-08 · 학회/저널 마감일
- **상태**: 미확인. **사용자 몫.**
- **차단 요인**: 정보 없음.
- **다음 행동**: 목표 학회·저널과 마감일 확정 → 남은 태스크(OPEN-06 30분, S4·S5 수 시간, S6, 원고 개정)를 역산해 일정 배치.

---

## 참고 — 완료된 관문

| 관문 | 결과 |
|---|---|
| T2 DoD (분절 수·SNR 오차·누수) | 통과. 8,280분절, SNR 오차 최대 2.9e-07 dB, 기록 교집합 0 |
| T5 ④ 비공식 대응 행렬 (go/no-go) | **조건부 통과.** 심장 vs 비심장 분리는 명확(clean \|r\| 0.555 vs 잡음 0.04), 잡음 3종 중 em은 분리, **bw·ma는 미분리** |
| 경계 120샘플 아티팩트 | 없음 (경계 0.2501 vs 중앙 0.2554, 비 0.98배) |
| 단위 테스트 | 33/33 통과 |
